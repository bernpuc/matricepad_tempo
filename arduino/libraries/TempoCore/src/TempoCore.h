#pragma once

// Umbrella header for the shared pieces every MatricePad Tempo sketch reuses:
// marquee scrolling, status icon/overlay drawing, and serial field parsing.
// Include the individual headers instead if a sketch only needs one piece.
#include "ScrollText.h"
#include "StatusIcons.h"
#include "SerialFraming.h"
#include "RotaryEncoder.h"
